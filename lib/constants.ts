// Number of ping measurements
export const PING_COUNT = 10;

// Download test duration in ms
export const DOWNLOAD_DURATION_MS = 10000;

// Upload test duration in ms
export const UPLOAD_DURATION_MS = 8000;

// Upload chunk size in bytes (512 KB)
export const UPLOAD_CHUNK_SIZE = 512 * 1024;

// Local storage key for history
export const HISTORY_STORAGE_KEY = "speed-test-history";

// Max history entries
export const MAX_HISTORY_ENTRIES = 20;

// Gauge max speed (Mbps)
export const GAUGE_MAX_SPEED = 1000;

// Update interval for live speed display (ms)
export const SPEED_UPDATE_INTERVAL_MS = 200;
